{
  "name": "kenya-paye-calculator-api",
  "version": "1.0.0",
  "description": "Fake API for PAYE Calculator using json-server",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
