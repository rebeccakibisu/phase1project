{
  "name": "kenya-paye-calculator-api",
  "version": "1.0.0",
  "description": "Backend for PAYE calculator using JSON server",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
